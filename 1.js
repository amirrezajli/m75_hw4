let zero = new Number(0);        
if (zero) {                         
 alert( "zero is truthy!?!" );
 
}

 //the output is tru & show alert,in console.log output is an object => {0} , because we use new Number(0)

 // if we use let zero= 0 , if(0)the output is false